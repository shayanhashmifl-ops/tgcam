python-telegram-bot==20.7  # or latest stable version
opencv-python-headless==4.9.0.80 # headless for server/bot environments
# or use just opencv-python if you need a GUI, but headless is safer for bots
